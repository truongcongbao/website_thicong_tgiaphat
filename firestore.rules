rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow full read, write, create, update, and delete access to all collections
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}
